server:
  port: 8080

spring:
  application:
    name: remain
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Mano@2001
    url: jdbc:mysql://localhost:3306/student
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true